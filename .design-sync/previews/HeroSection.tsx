import { HeroSection } from 'roboledger-app'

/** The full landing hero: gradient headline, value proposition, and the primary
 *  call-to-action ("Built on RoboSystems"). Full-bleed on a black canvas — give
 *  the card a tall viewport so the section fills. */
export const Default = () => <HeroSection />
