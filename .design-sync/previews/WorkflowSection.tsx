import { WorkflowSection } from 'roboledger-app'

/** The end-to-end ledger → close → report workflow walkthrough — how data moves
 *  from connected sources through the books to filed financial reports. */
export const Default = () => <WorkflowSection />
