import { PlatformSection } from 'roboledger-app'

/** "Powered by RoboSystems" — the RoboLedger knowledge-graph schema and the
 *  platform foundation the product is built on. Full-bleed. */
export const Default = () => <PlatformSection />
