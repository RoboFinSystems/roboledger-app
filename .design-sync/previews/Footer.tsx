import { Footer } from 'roboledger-app'

/** Site footer — product and applications columns, social links, and legal
 *  line. Sits on the dark canvas at the bottom of the landing page. */
export const Default = () => <Footer />
