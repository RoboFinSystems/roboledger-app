import { FinalCTA } from 'roboledger-app'

/** The closing call-to-action band — gradient panel inviting sign-up, at the
 *  foot of the landing page above the footer. Full-bleed. */
export const Default = () => <FinalCTA />
