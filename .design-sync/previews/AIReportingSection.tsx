import { AIReportingSection } from 'roboledger-app'

/** AI-powered reporting walkthrough — a natural-language question resolved into
 *  a financial insight drawn from the ledger. Full-bleed. */
export const Default = () => <AIReportingSection />
