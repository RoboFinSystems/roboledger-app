import { FeaturesSection } from 'roboledger-app'

/** The before/after pitch — the old "Publish & Pray" close vs. RoboLedger's
 *  event-driven "Events Flow In → Publish & File" flow, as a capability grid. */
export const Default = () => <FeaturesSection />
