import { OutputFormats } from 'roboledger-app'

/** The output formats RoboLedger produces — the filing/report deliverables the
 *  ledger generates, presented as a format gallery. Full-bleed. */
export const Default = () => <OutputFormats />
